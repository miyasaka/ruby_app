<%= form_for(@sample2) do |f| %>
  <% if @sample2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sample2.errors.count, "error") %> prohibited this sample2 from being saved:</h2>

      <ul>
      <% @sample2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :col1 %><br>
    <%= f.text_area :col1 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
